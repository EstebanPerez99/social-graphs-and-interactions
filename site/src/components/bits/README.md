Components copied from [React Bits](https://reactbits.dev) (MIT, © David Haz) — TypeScript + Tailwind variant.
Copied by hand on purpose: the site is Astro, so each one is used as a React island (`client:visible` / `client:load`).
Do not edit these beyond what DESIGN.md allows; add new ones here with the same name as upstream.

Local edits: `SpotlightCard` — removed the dark base classes so it takes `.panel` from the caller.
